# keys/

Run `python src/crypto_utils.py` once to generate `private_key.pem` and
`public_key.pem` here. Commit both -- per the assignment spec (Section 9), demo-only
keys don't need to stay secret. Everyone on the team should use this same keypair
so signatures made by one person verify correctly for everyone else.
